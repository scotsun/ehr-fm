# HAT Model Dependencies
# For GPU: install torch first with CUDA
# pip install torch --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
pandas>=2.0.0
pyarrow>=12.0.0
duckdb>=0.9.0
tokenizers>=0.15.0
mlflow>=2.8.0
transformers>=4.30.0
